# Command-line arguments override these values. Any omitted setting uses the
# corresponding default defined in api_server.py.
# The API credential is intentionally not stored here. Set HUNYUAN3D_API_KEY
# in the process environment or systemd EnvironmentFile.
host: 0.0.0.0
port: 8080
model_profile: fast
tex_model_path: models/Hunyuan3D-2
device: cuda
enable_tex: true
idle_timeout: 300
